export enum PATH {
  LOGIN = '/login',
  REGISTER = '/register',
  DASHBOARD = '/dashboard',
  ACCOUNTS = '/dashboard/accounts',
  ADD_ACCOUNT = '/dashboard/add-account',
  CALENDAR = '/calendar',
  DAY = '/day',
  ORDER_ACCOUNTS = '/dashboard/accounts/order-accounts',
}

export enum STORAGE {
  AMOUNT_PER_DAY = 'amountPerDayConfig'
}

